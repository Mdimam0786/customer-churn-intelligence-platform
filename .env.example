# Copy this file to .env and fill in real values before connecting to a
# live PostgreSQL instance (see docs/technical_design_document.md Section 2.2
# for why this sandbox used a local SQLite stand-in instead).

DATABASE_URL=postgresql://username:password@localhost:5432/churn_intelligence_platform
